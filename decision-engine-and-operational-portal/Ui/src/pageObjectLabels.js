const button = "button";

const common = {
  buttons: {
    open: `${button}.open`
  }
};

const layout = {
  menu: {
    navLink: "navLink",
    openButton: common.buttons.open,
    panel: "panel.mainMenu",
    toolbar: "toolbar"
  }
};

export default {
  layout
};
